#include <stdio.h>

int main(void)
{

  /* Declare
   * the
   * variables
   */
  int loop_index = 0;

  while(loop_index < 5)
    {
      printf("You'll see this five times!\n");
      loop_index++;
    }

  return 0;
}
